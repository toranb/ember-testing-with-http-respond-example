// model: function(){
// return this.get("user").get_user_details(access_token).then(function() {
// return self.promise(url, access_token).then

var LoginRoute = Ember.Route.extend({
    model: function() {
        return [];
    }
});

export default LoginRoute;
